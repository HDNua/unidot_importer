fbx2gltf*
FBX2glTF*
unity_asset_database.*res
static_cache.*res
static_storage.*res

# Python bytecode generated by validation tools and tests.
__pycache__/
*.py[cod]

# AI working notes — session transcripts, plans, scratch. Never published from
# this repo: they quote private paths and downstream context verbatim, and the
# repo is a public fork.
0_ai/
